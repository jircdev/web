# Aprendizaje en HTML

1. Etiquetas o Selectores.
2. Atributos.
3. Encabezados.
4. Enlaces.
5. Imagenes: etiquetas `<img>`, `<picture>`, `<figcaption>`, `<figure>`.
    - Selector `<picture>`.
        - Atributos `srcset`, `sizes`.
6. Lista `<ul>`, `<ol>`, y `<li>`.
7. Tablas `<table>`, `<thead>`, `<tbody>`, `<th>`, `<td>` y `<tr>`.
8. Formularios.
    - Tipos de inputs.
    - Textarea
    - Select
    - Atributos
9. Iframes
10. Video: Etiqueta `<video>` y `sources`.
    - atributos `src` y `controls`
11. Componentes Web.
